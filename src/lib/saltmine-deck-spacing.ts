/**
 * Shared spacing for Saltmine desktop deck embeds (880×530).
 * Tune booking timeline rhythm here — components import these class constants.
 */

/** Vertical gap between day sections in the booking timeline scroll region. */
export const SALTMINE_DECK_TIMELINE_GAP_CLASS = "space-y-3.5";

/** Vertical rhythm inside a day section (header → presence → bookings → CTA). */
export const SALTMINE_DECK_DAY_SECTION_GAP_CLASS = "space-y-2";

/** Gap between booking timeline groups (All day / Up next). */
export const SALTMINE_DECK_BOOKING_GROUPS_GAP_CLASS = "space-y-2.5";

/** Gap between group label and cards, and between cards in a group. */
export const SALTMINE_DECK_BOOKING_GROUP_GAP_CLASS = "space-y-1.5";

/** Booking card inner padding. */
export const SALTMINE_DECK_BOOKING_CARD_PAD_CLASS = "px-2.5 py-2.5";

/** Presence row inner padding (default). */
export const SALTMINE_DECK_PRESENCE_ROW_PAD_CLASS = "px-2.5 py-1.5";

/** Presence row inner padding (compact embeds). */
export const SALTMINE_DECK_PRESENCE_ROW_PAD_COMPACT_CLASS = "px-2 py-1";

/** Main bookings panel padding (desktop embed). */
export const SALTMINE_DECK_MAIN_PAD_CLASS = "px-3.5 py-3";

/** Main bookings panel padding (mobile embed inside dashboard frame). */
export const SALTMINE_DECK_MAIN_PAD_MOBILE_CLASS = "px-2.5 py-2.5";
